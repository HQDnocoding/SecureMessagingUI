version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=https://localhost:7000
    networks:
      - securechat-network
    restart: unless-stopped

networks:
  securechat-network:
    driver: bridge